34.149： /data/dingzher/DexGrasp_Demo：

1. RosBag transfer to Zarr Format：

/data/dingzher/DexGrasp_Demo/SRCB-DexVLA/zarr_data_transfer/rosbag_zarr_transfer.py

更改{directory}对应转换的zarr数据集保存的路径为{zarr_output_dir}.

2. add mask to zarr format dataset:

/data/dingzher/DexGrasp_Demo/SRCB-DexVLA/get_mask.py
更改{input_path}，运行文件后点击bbox，之后在对话框中输入c就保存这个bbox，如果要更换就输入其他的键，输出加mask的zarr格式的数据集也为{input_path}

​	3.训练文件：
/data/dingzher/DexGrasp_Demo/SRCB-DexVLA/DexGraspVLA/controller/config/task/grasp.yaml
修改对应的dataset_paths为你想训练的文件。
/data/dingzher/DexGrasp_Demo/SRCB-DexVLA/DexGraspVLA/controller/config/train_dexgraspvla_controller_workspace.yamltime_name：每次训练的时候修改一下time_name。

4. 集群上的训练命令

 ~~~
 source ~/.bashrc
 conda activate DexGrasp
 cd /common-data/train_code/DexGraspVLA
 accelerate launch --num_processes=4 train.py --config-name train_dexgraspvla_controller_workspace
 ~~~

 5. 输出的checkpoint及log文件：

    /data/dingzher/DexGrasp_Demo/data/{time_name}

    /data/dingzher/DexGrasp_Demo/data/outputs/train_dexgraspvla_controller_id_1